// ===========================================
// Librerías estándar de C++ utilizadas en el proyecto
// ===========================================

#include <iostream>   // Entrada/salida estándar (cout, cin, etc.)
#include <sstream>    // Operaciones con strings como si fueran streams
#include <vector>     // Contenedor dinámico de tipo vector
#include <string>     // Soporte para cadenas de texto (std::string)
#include <iomanip>    // Manipuladores de I/O (como setw, setfill, hex)
#include <cctype>     // Funciones para manejo de caracteres (isprint, isspace)
#include <algorithm>  // Algoritmos generales (all_of, transform, etc.)
#include <bitset>     // Representación y manipulación de bits (std::bitset)

using namespace std;  
